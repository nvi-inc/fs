! Common array that holds different track assignments.
      include '../skdrincl/skparm.ftni'
      integer max_val,num_val
      parameter(max_val = max_stn * max_frq * max_headstack * max_chan)
!      parameter(max_val=20000)    !way big.
      integer*4 indvec(max_val)
      integer   itrasvec(max_val)
      logical khead(max_headstack,max_stn)  !set to true if using headstack.

      common /itras_cmn/num_val,indvec,itrasvec,khead


