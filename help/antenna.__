antenna - direct line to the antenna
Syntax:      antenna=message,message, ...

Response:    antenna/response,response, ... 


Settable parameters:
message         message to be sent to the antenna in the exact
                form required by the pointing software. Each
                message between commas will be sent
                separately.

Monitor-only parameters:
response        response of the antenna to the message. This
                response is either ack or an error message sent
                by the antenna.

