LO - Specify first LO frequencies

Syntax:   LO=<1nor>,<2nor>,<1alt>,<2alt>
Response: none

Settable parameters:
<1nor>: Frequency of Channel 1, normal input, MHz. Default 0.
<2nor>: Frequency of Channel 2, normal input, MHz. Default 0.
<1alt>: Frequency of Channel 1, alternate input, MHz. Default 0.
<2alt>: Frequency of Channel 2, alternate input, MHz. Default 0.

      Comments: This command simply stores the LO frequencies for
access by program PCALR. These values along with the video
converter frequencies are used to calculate the phase calibrator
tone frequencies. The PATCH command should also be used to specify
which converters are on each LO.
