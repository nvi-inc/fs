LOGOUT - Set log output unit(s)

Syntax:   LOGOUT=<unit>,<unit>,...
Response: none

Settable parameters:
<unit>: Logical unit (>0) for display of log entries. Up to 5 units
        may be specified. Default is the operator's terminal.

      Comments: Log entries are always written into the log file,
this command is only for display during operations. All log entry
display may be suppressed by issuing the command LOGOUT=0.
