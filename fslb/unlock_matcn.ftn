FTN77,Y,I
$CDS ON
$ALIAS /FSCOM/ , NOALLOCATE
$ALIAS RNRQ, NOABORT
      SUBROUTINE UNLOCK_MATCN(IP)
      INTEGER*2 IP(5)
C
      INCLUDE /fs/include/fscom.FTNI
C
      CALL RNRQ(40004B,IRNMATC_FS,ISTAT,*30)
30    CONTINUE
      IF(ISTAT.NE.1) THEN
        PAUSE 'FATAL ERROR IN UNLOCK_MATCN'
      ENDIF
C
      RETURN
      END
